# MULTIMIND DESIGN-DNA — MIGRATION GATE / IMPLEMENTATION MASTER

Status: ACC KEEP MASTER / PLANNING LOCKED
Date: 2026-09-05

This file is the durable cross-chat anchor for the Design-DNA implementation chapter.

## Authoritative state

```text
DESIGN_DNA_RESEARCH = CLOSED
GLOBAL_DENOMINATOR = 271
GLOBAL_EQ3 = 271/271
EQ4 = 0/271
DOCUMENTATION = MERGED_TO_MAIN
MIGRATION_PLANNING_GATE = PASS
HARD_DESIGN_DNA_BLOCKERS = 0
DESIGN_DNA_IMPLEMENTATION = HOLD
PRODUCTION_MIGRATION_AUTHORIZED = NO
```

Do not restart closed Design-DNA research, calibration, census, archival recovery, Waves E/F/G/H, Track M_R, Track T/T-I, or denominator work without new material evidence.

## Governor decision

Design-DNA planning is accepted and locked. The implementation hold is an integration-order constraint, not a Design-DNA research defect.

Current release condition:

```text
RJ-1 CLOSED + INTEGRATED
→ RJ-2 REFLEX PRODUCTION HOST CLOSED + ACCEPTED
→ EXPLICIT DESIGN-DNA M0 IMPLEMENTATION AUTHORIZATION
```

This file does not itself authorize Codex or implementation.

## Integration doctrine

Design-DNA is a presentation projection system above semantic application state, not a second backend, CSS-theme catalogue, or application core.

```text
Semantic Application State
        +
Selected DNA Composition
        ↓
Deterministic Resolver
        ↓
Semantic Presentation Projection
        ↓
Host Adapter / Reflex Presentation
        ↓
Rendered MultiMind
```

Reference/archetype presentation behavior must not leak into AI/provider/core semantics. Do not introduce a separate HTTP/FastAPI service merely for Design-DNA.

## Low-maintenance architecture law

The 271 calibrated units must scale primarily as contracts/data, not 271 bespoke implementations.

```text
DNA DEFINITIONS
→ REGISTRY / MANIFEST LOADER
→ VALIDATOR
→ DETERMINISTIC RESOLVER
→ SEMANTIC PROJECTION
→ HOST ADAPTER
```

Permanent firewall:

```text
REFERENCE != ENGINE != PRIMITIVE != FIXTURE != ASSET
```

Prefer one registry/loader, one resolver, one projection contract, centralized primitives/tokens, typed/validated contracts, one asset eligibility/provenance system, structural fallback, progressive asset enrichment, reusable fixtures, and minimal per-reference code.

Reject as canonical architecture: 271 bespoke UI components; 271 independent CSS themes; giant per-reference condition chains; DNA branching in core; mandatory assets for identity; duplicate Streamlit/Reflex DNA engines; duplicated mobile theme definitions where projection can derive them; DB expansion merely for theme rendering; flat 271-item selector.

If adding a conforming DNA reference after M0–M2 still requires substantial bespoke implementation, repair the machinery before scaling out.

## Operational sequence

The canonical M0–M12 map remains authoritative packaging.

### M0 — Runtime machinery

Build the minimum reusable runtime: schema/model boundary, registry/manifest loading, validation, deterministic resolver, semantic zones, ownership, dominance/contradiction handling, degradation, accessibility veto, Reading Sanctuary, reduced motion, responsive/mobile recomposition, selection/composition state, A→B→A preservation, asset-state abstraction, provenance/explainability, and test harness.

### M1 — Engine proving

Material M1–M15 + Environment E1–E14 = 29 engines. Prove reusable engine composition before broad rollout.

### M2 — Structural identity proving slice

Country-Web 5 + selected adversarial Cultural Tier-S representatives + only required primitives. Prove morphology/hierarchy/rhythm can change without wallpaper/stereotype behavior, state mutation, or asset dependence. M2 is a hard scale-out checkpoint.

### M3 — Cultural Tier S

16 active references.

### M4 — Complete primitive families

P01–P25 + MK01–MK25 + TP01–TP18 = 68 primitives.

### M5 — Izzul Personal Media

Anime 15 + Manga 11 + Manhwa 10 = 36 references.

### M6 — Miko Personal Media

23 references. Group only from authoritative evidence; do not invent symmetry with Izzul.

### M7 — Izzul Music / Temporal

Track T-I = 16 references using TP machinery. Temporal/music DNA governs presentation behavior; it does not require playback/distribution of protected recordings.

### M8 — Cultural Tier A

29 active references.

### M9 — Cultural Tier B

10 active references.

### M10 — Track M_R

25 named/reconstructed material references. Preserve `M1–M15 != TRACK_M_R != HISTORICAL TRACK M`.

### M11 — Full fixture torture

14 additive fixtures. Test resolver collisions, deterministic selection, desktop morphology, mobile recomposition, accessibility, Reading Sanctuary, reduced motion, asset-off identity, partial asset failure, fallback, explainability/provenance, state continuity, and A→B→A switching.

### M12 — Asset enrichment

Only after structural/asset-off projection is healthy.

```text
ASSET_APPLICABLE = 207
ASSET_NOT_APPLICABLE = 64
ASSET_FINAL_APPROVED = 0
DIRECT_IP_GATED = 75
```

Do not block structural deployment merely because all applicable assets lack final selection. Production-web eligibility remains license/provenance gated.

## Relationship to Reflex migration

Design-DNA joins the production migration rather than becoming a third independent migration program.

```text
RJ-1 generic seams
→ RJ-2 accepted Reflex host
→ DESIGN-DNA M0 START LINE
→ RJ-3 + Design-DNA runtime/presentation integration
→ DNA scale-out + deployment/parity work
→ M11 aligns with RJ-5 torture/parity evidence
→ RJ-6 cutover/rollback
→ FINAL GOVERNOR MIGRATION GATE
```

RJ and M batches keep separate ownership/evidence while sharing one presentation architecture.

## Selector law

`271 CALIBRATED UNITS != 271 USER-FACING THEME BUTTONS`.

Natural user-facing families: Cultural Tier S/A/B; Country/Web; Personal Media Izzul Anime/Manga/Manhwa; Miko evidence-supported groups; Music/Temporal Izzul; Track M_R only where deliberately exposed. Engines/primitives are primarily internal; fixtures are test-only.

## Per-batch law

```text
SELECT
→ IMPLEMENT
→ TARGETED TEST
→ ADVERSARIAL / RUNTIME TEST
→ GOVERNOR REVIEW
→ PATCH FAILED UNITS / OWNING MACHINERY
→ ACCEPT
→ LOCK
→ NEXT
```

A later failure does not reset earlier accepted batches unless new evidence invalidates them. Do not run one uncontrolled 271-unit mega-rewrite.

## EQ4 doctrine

EQ3 means implementation-ready specification, not runtime proof. EQ4 begins only after authorized real-host implementation.

Applicable EQ4 evidence includes manifest validation, deterministic resolver behavior, owned/non-owned zones, desktop projection, mobile recomposition, accessibility, reduced motion, Reading Sanctuary, asset-off identity, valid asset enrichment where available, safe partial asset failure, semantic-state preservation, A→B→A continuity, deterministic collisions/fallbacks, traceable provenance, and fixture/regression proof.

`GLOBAL_EQ3 = 271/271` must never be reported as migration completion.

## Deployment objective

```text
DEPLOYABLE
+ BORING TO OPERATE
+ LOW MAINTENANCE
+ STRUCTURALLY RESILIENT
```

Favor reusable machinery over per-reference cleverness, deterministic contracts over hidden heuristics, structural fallback over hard asset dependency, and one production renderer over parallel host-specific DNA systems.

## Current gate

```text
DESIGN_DNA_RESEARCH = CLOSED
GLOBAL_EQ3 = 271/271
EQ4 = 0/271
MIGRATION_PLANNING_GATE = PASS
LOW_MAINTENANCE_ARCHITECTURE_DIRECTION = LOCKED
M0–M12 OPERATIONAL MAP = RETAINED
DESIGN_DNA_IMPLEMENTATION = HOLD
CODEX_DESIGN_DNA_AUTHORIZED = NO
PRODUCTION_MIGRATION_AUTHORIZED = NO
HARD_DESIGN_DNA_BLOCKERS = 0
```

Release condition remains:

```text
RJ-1 CLOSED + INTEGRATED
→ RJ-2 CLOSED + ACCEPTED
→ explicit implementation authorization
→ M0
```

## Cross-chat handoff

Read in order:
1. `docs/design-dna/README.md`;
2. this file;
3. `docs/design-dna/migration/MIGRATION_BATCH_MAP.md`;
4. current Reflex production migration master and current repository implementation state.

Do not infer RJ completion from this file. Verify current repository/accepted RJ checkpoint before authorizing M0.

---

Governor decision: **ACC KEEP MASTER**.

This lock authorizes continuation of Design-DNA migration planning. It does not authorize Codex, Design-DNA implementation, production cutover, or EQ4 claims.