# MULTIMIND DESIGN DNA — VERBATIM ARCHIVE ACC KEEP LOCK CHECKPOINT v1

STATUS: ACC KEEP MASTER / LOCKED
DATE: 2026-09-04
AUTHORITY: USER + DESIGN DNA HQ / PROJECT GOVERNOR
SCOPE: DOCUMENTATION CONSOLIDATION / RAW VERBATIM ARCHIVE
CODEX: NOT INVOKED
PRODUCTION: UNTOUCHED
MIGRATION: NOT AUTHORIZED
EQ4: NOT PERFORMED

## 1. LOCKED USER DECISION

User explicitly requires:

`SEMUA KATA DARI SEMUA MD LAMA HARUS MASUK GITHUB.`

This supersedes the earlier interpretation that semantic/governance losslessness alone was sufficient and that raw byte/verbatim mirroring was nonblocking.

## 2. AUTHORITATIVE ARCHIVE LAW

Documentation closure now requires two simultaneous layers:

A. CANONICAL CURATED KNOWLEDGE BASE
- current truth;
- reconciled governance;
- modular navigation;
- explicit supersession;
- migration-readable structure.

B. RAW VERBATIM HISTORICAL ARCHIVE
- every recoverable historical Design-DNA Markdown artifact preserved in full;
- source text not summarized, cleaned, normalized, or silently corrected;
- superseded artifacts preserved;
- failed research preserved;
- dead routes preserved;
- branch briefs preserved;
- old checkpoints/locks preserved;
- duplicate candidates preserved until identity is proven.

Canonicalization does not replace source preservation.

## 3. REQUIRED DESTINATION

`docs/design-dna/archive/raw/`

Every archived artifact must also be represented in a source manifest/registry with its retrieval and verification state.

## 4. VERIFICATION STATES

- `RAW_ARCHIVED_VERIFIED` — full source content was retrievable and copied without intentional omission.
- `RAW_ARCHIVE_PENDING_FULL_SOURCE_ACCESS` — source is known but retrieval is truncated/incomplete.
- `DUPLICATE_CANDIDATE` — same or similar name/content exists but full identity is not yet proven.
- `DUPLICATE_VERIFIED` — sufficient full-content/byte identity was established.
- `SOURCE_NOT_YET_DISCOVERED` — expected historical family/member not yet located.

Snippets are not full-source evidence. Missing words must never be reconstructed from memory.

## 5. MIGRATION FIREWALL

`FULL_DOCUMENTATION_ZERO_LOSS_AUDIT = BLOCKING GATE`

Migration Gate Review remains blocked until every recoverable historical Markdown artifact is either:

1. `RAW_ARCHIVED_VERIFIED`; or
2. individually documented as unresolved because full source access is genuinely unavailable.

No unresolved source may be silently treated as archived.

## 6. CURRENT DEEP OPERATION

Governor is authorized to continue immediately, family-by-family:

`DISCOVER → MCICK/FULL-READ → VERIFY RETRIEVAL COMPLETENESS → RAW ARCHIVE → MANIFEST → DUPLICATE/SUPERSESSION RECONCILIATION → NEXT`

No Codex. No production mutation. No Reflex migration. No EQ4.

## 7. LOCK VERDICT

`VERBATIM_ARCHIVE_REQUIREMENT = ACC KEEP MASTER / LOCKED`

`DEEP_RECOVERY = AUTHORIZED`

`SEMANTIC_CONSOLIDATION = PASS / RETAINED`

`FULL_DOCUMENTATION_ZERO_LOSS = OPEN UNTIL RAW ARCHIVE COMPLETE`

`MIGRATION_GATE = BLOCKED`
